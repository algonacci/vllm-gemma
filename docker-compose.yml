services:
  vllm-gemma3-4b-it:
    image: vllm/vllm-openai:latest
    container_name: vllm-gemma3-4b-it
    command: >
      --model google/gemma-3-4b-it
      --host 0.0.0.0
      --port 8000
      --max-model-len 65536
      --gpu-memory-utilization 0.8
      --chat-template /app/chat_template_gemma3.jinja
    ports:
      - "5095:8000"
    environment:
      - HUGGING_FACE_HUB_TOKEN=${HUGGING_FACE_HUB_TOKEN}
    volumes:
      - ~/.cache/huggingface:/root/.cache/huggingface
      - ./chat_template_gemma3.jinja:/app/chat_template_gemma3.jinja:ro
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: ["gpu"]
    restart: unless-stopped
